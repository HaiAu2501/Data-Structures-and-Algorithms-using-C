#include <bits/stdc++.h>

using namespace std;

// Cho danh sách n việc và k người. Tìm thời gian nhỏ nhất để hoàn thành.

#define MAX 15

int n, k, jobs[MAX];

int main()
{
    cin >> n >> k;
    for (int i = 1; i <= n; i++)
        cin >> jobs[i];
}